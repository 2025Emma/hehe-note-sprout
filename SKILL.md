---
name: hehe-note-sprout
description: >
  当用户要给一张笔记「发芽」、找关联、补视角、做拷问时使用。任意笔记均可：Obsidian 卡片、普通 markdown 文件、直接粘贴的文字；Obsidian 等 wikilink 工具用户获得完整体验。
  触发词：发芽、给这张卡片发芽、笔记发芽、找关联、补充视角、压力测试这张笔记。
version: v2.0
---

# 发芽 Skill (hehe-note-sprout)

## 用途

给任意一张笔记执行「本地发芽 → 外部发芽 → 专家拷问」三层循环，生成一份结构化发芽报告，帮助碎片想法长成有根有据的永久笔记。

**核心原则：不修改原笔记，只做连接与启发。**

**设计理念**：发芽不是分类信息，而是**从记录中生长洞察**；拷问不是结构化审查，而是**帮你把想法想透**。

**不挑笔记格式**：Obsidian 卡片、普通 markdown 文件、直接粘贴的一段文字都可以。有笔记库时三层全开；没有库时自动走「外部发芽 + 专家拷问」两层模式；Obsidian / Logseq / Foam 等支持 wikilink 的工具用户获得完整体验（含发芽标记）。

---

## 配置（可选，开箱即用）

本 Skill **无需配置即可使用**。发芽报告默认生成在**原笔记所在目录**，与笔记放一起，就近查看。

> 没有笔记库、只想发芽一段文字？直接粘贴内容即可（走「外部发芽 + 专家拷问」两层模式）。

**可选：自定义跳过目录**（全库搜索 / 链接同步时忽略，默认已跳过 `.obsidian`、`.trash`、`.git`、`node_modules`）。有大型归档或收件箱目录时可告诉 AI，例如：

```
全库搜索和链接同步时，跳过我的 Archives/ 和 Inbox/ 目录
```

**文件名约定（可选）**：若笔记文件名带 `YYYYMMDD-` 日期前缀，提取主题时会自动去掉；没有前缀也不影响使用。

---

## 触发条件

当用户提到以下任一表达时触发：

- 「给这张卡片发芽」
- 「笔记发芽」
- 「找关联」
- 「补充视角」
- 「压力测试这张笔记」
- 「这张笔记能长出什么」
- 「哪些笔记还没发芽 / 所有没发芽的都发芽」（→ 走下面的**批量发芽模式**）

---

## 批量发芽模式（一次发多张笔记）

> 批量发芽针对**用户指定的目录**（如「把 xxx 目录下的笔记都发芽」）；两层模式（无笔记库）下无批量发芽。

当用户要求「把没发芽的都发芽」「批量发芽」时，走这套流程，不要一张张串行做。

### Step 1 — 判定谁没发芽
- **用户指定扫描目录**（不指定则先询问）。
- **判定标准：文件名末尾带 `🌱` = 已发芽**（并应有对应发芽报告）。没有 🌱 的就是待发芽。
- **排除发芽报告**：跳过文件名以 `-发芽报告.md` 结尾的文件（那是已生成的报告，不是笔记——报告与笔记同目录时尤其要区分）。
- 输出清单：已发芽 N 张 / 待发芽 M 张。

### Step 2 — 按主题分组
不要按日期顺序切，按**主题聚类**分组（同一组的卡片彼此容易做本地洞察的交叉引用）。每组 3-4 张。

### Step 3 — 派 subagent 并行，但控制并发
- 每组一个 subagent，prompt 必须**自包含**：内嵌三层循环规范 + 完整输出模板 + 铁律 + 该组笔记路径 + 报告输出路径（= 各自笔记所在目录）。
- **⚠️ 并发建议从 2 个开始**。原作者环境实测同时派 4 个会撞 429 / 502；不同 Agent 框架和模型配额差异很大，请按自己的环境调整。
- **⚠️ 报错不等于任务没跑**：agent 可能返回错误但文件已写出。每批结束后用 `ls`/`wc -c` 回目录核对实际产出，再决定补派谁。

### Step 4 — 交叉引用
告诉每组 subagent：同批处理目录里已有/正在生成同批报告，本地洞察里**可以互相点名**（例："《笔记A》发芽时发现的'闭环能力'主题，在这张笔记里又出现了"）。这让整批报告之间长成网络，而不是一座座孤岛。

### Step 5 — 收尾 + 发芽标记
- 核对报告数量 = 待发芽数量 + 原有报告数。
- 报告生成后，进入「发芽标记」流程（见下方独立章节）。**首次执行必须先向用户确认**（该操作会重命名文件并全库更新 wikilink）；用户明确授权后，后续批次可默认执行。
- 核对原卡片 mtime：标记动作会改文件名与末尾区块，属预期内改动，不算"误改正文"。
- 仅当用户当次明确说"只生成报告、不要改卡片"时，才跳过标记步骤。

---

## 发芽标记（加🌱 + 末尾追加区块）

> **适用范围**：仅适用于 Obsidian / Logseq / Foam 等支持 wikilink 的工具环境；其他环境跳过本节。
>
> **⚠️ 破坏性提示**：本操作会**重命名原卡片文件**（加 🌱 后缀）并**全库更新 wikilink**。首次执行前必须向用户说明并征得同意；用户授权后可作为默认收尾动作，无需逐次再问。用户说"只出报告不改卡片"时跳过。

**何时触发**：每生成一份发芽报告，且用户已授权标记动作后，立即对对应的原卡片执行以下流程（单张或批量同理）。

### 1. 构建改名映射（避免重复/误伤）
- 由同目录下的报告 `{YYYYMMDD-主题}-发芽报告.md` 推导笔记基名 `{YYYYMMDD-主题}`。
- 若 `{基名}.md` 存在且 `{基名}🌱.md` 不存在 → 纳入改名映射 `基名 → 基名🌱`。
- 若 `{基名}🌱.md` 已存在 → 视为已发芽，跳过（不重复加 🌱、不重复追加区块）。

### 2. 全库同步 wikilink（防断链，先改链接再改名）
- 遍历整个 vault 的 `*.md`，排除配置中的排除目录（默认 `.obsidian`、`.trash`、`.git`、`node_modules`）。
- 对每张待发芽笔记，用正则 `\[\[{基名}(?=[\]\#|])` 替换为 `[[{基名}🌱`，**保留**后续 `]]` / `#标题` / `|别名`。
- **关键防双🌱**：lookahead 限定后接字符必须是 `]` `#` `|`，这样 `[[{基名}🌱]]` 不会被二次命中。
- 普通笔记、日记、其他发芽报告（含其 frontmatter `source_note` 与正文 `来源笔记`）、已发芽卡片的区块引用，全部一并更新。

### 3. 改名 + 追加区块
- 把 `{基名}.md` 重命名为 `{基名}🌱.md`。
- 在文件**末尾追加**如下区块（不改动正文任何已有内容）：

```markdown
---

## 🌱 发芽

🌱 [[{YYYYMMDD-主题}-发芽报告|发芽报告]]

### 相关笔记

**已有连接**
- [[{本笔记正文中已链接的其他笔记}🌱]]

**建议建立的新连接**
- [[{从发芽报告的「本地笔记的洞察」中提取的其他笔记}🌱]]
```

- **已有连接**：扫描本笔记正文中的 `[[...]]`，筛出指向笔记库内其他笔记的链接（去掉自身）。
- **建议建立的新连接**：扫描对应发芽报告中的 `[[...]]`，筛出指向笔记库内其他笔记的链接（去掉自身、去掉已在"已有连接"中的，上限 6 条）。
- 两栏**互斥**：建议栏不得包含已有连接栏已出现的笔记（去重）。
- 某栏为空时写占位：`（本笔记暂未链接其他笔记）` / `（见发芽报告中「本地笔记的洞察」）`。

### 4. 幂等保障
- 重跑安全：已改名为 `🌱` 的卡片不在改名映射内；链接正则的 lookahead 防双🌱；区块只在改名映射内的文件上追加（已发芽文件不会被二次追加）。

---

## 输入

用户指定一张卡片笔记路径，或粘贴笔记内容。

**优先读取路径**：如果用户提供了文件路径，先用 `Read` 工具读取完整内容。如果用户只提供了主题名，在用户所在笔记库（或指定目录）中搜索匹配的文件。

**没有笔记库（两层模式）**：用户直接粘贴内容、或没有可搜索的笔记库时，跳过第一层本地发芽，只走「外部发芽 + 专家拷问」；报告中省略「本地笔记的洞察」章节，发芽建议全部基于外部发现与拷问。

---

## 核心工作流：三层循环

### 第一层：本地发芽（Local Sprouting）

**目标**：在知识库中找到与当前卡片相关的笔记。

> **适用范围**：本层需要一个可搜索的笔记库（任意文本文件目录即可）。非 wikilink 环境下，「已有连接反向搜索」维度自动跳过，其余搜索照常；用户没有笔记库时，整层跳过，走「外部发芽 + 专家拷问」两层模式。

**Step 1 — 提取种子关键词**：
1. 读取笔记 frontmatter 的 `tags`
2. 去掉日期前缀（如有），提取标题核心概念
3. 提取正文中的 `[[wikilink]]` 作为已有连接点
4. **通读全文**，提取 5-10 个核心关键词（名词、概念、主题词），按重要性排序

**Step 2 — 多路搜索**（全部用 `Grep` 工具）：

| 通道 | 搜索范围 | 权重 |
|------|---------|------|
| 已有连接反向搜索 | 哪些笔记通过 `[[当前笔记名]]` 链接到本笔记 | 最高 |
| 标签匹配 | 搜索全库具有相同 `tags` 的笔记 | 高 |
| 标题匹配 | 用种子关键词搜索笔记标题/文件名 | 高 |
| 全文关键词 | 用种子关键词搜索正文，返回 Top 20 | 中 |

**Step 3 — 关联度排序**：
1. 已有双向 wikilink > 单向被引用 > 标签匹配 > 标题匹配 > 正文关键词
2. 同目录（与原笔记同目录）优先
3. 排除自身与配置中的排除目录
4. 最终输出：Top 5-8 张最相关的本地笔记

**Step 4 — 关联摘要**：
对每张关联笔记提取：文件名、标题、1-2 句核心观点、关联理由，标注「已有连接」vs「建议建立的新连接」。

> **注意**：本地发芽的发现**同时出现在两个地方**：
> 1. **发芽报告中「本地笔记的洞察」章节**——用叙述性洞察展示关联网络的全貌
> 2. **原文末尾的「发芽」区块**（用户授权标记后）——方便日常快速查看
>
> 报告中的「本地笔记的洞察」章节要写出**关联的深度解读**，不只是列链接——要解释"这张笔记和那篇笔记之间有什么关系"、"它们放在一起会产生什么新洞察"，并且**每条洞察都附上分析所依据的原文链接**。

---

### 第二层：外部发芽（External Sprouting）

**目标**：用跨领域故事串讲的方式，从互联网上找到与笔记核心概念相关的权威案例，补充本地知识盲区，生长出"Aha 瞬间"。

**核心原则**：
- **不是分类汇总**，而是**故事串讲**
- **每个故事都有种子引子**（"因为笔记中提到了XX，这让我想到YY"）
- **每个故事都要完整展开**（起承转合，不是一句话概括）
- **每个故事结尾都有 Aha 瞬间**（从案例中自己提炼的洞察，不是引用别人的金句）
- **每个 Aha 瞬间之后必须附「📎 参考文档链接」**（可追溯的 URL，方便用户追溯）
- **跨领域跨度越大，冲击力越强**

**Step 1 — 构建搜索查询**：

从种子关键词中选出 1-3 个核心概念，构建两类查询：

**A. 同领域深入查询**（根据笔记类型）：

| 笔记类型 | 查询模板 |
|---------|---------|
| 观点类 | `{主题} 不同观点 争议 批评` |
| 概念类 | `{主题} 定义 权威解释 学术` |
| 经验类 | `{主题} 案例 实践 方法论` |
| 作品类（电影/书） | `{作品名} 深度分析 多角度 评价` |

**B. 跨领域查询**（主动跳出原领域）：
- 从笔记内容判断可能涉及的其他领域（如心理学、经济学、工程学、传播学、历史学等）
- 构建查询：`{主题} {跨领域名称} 视角 / 解读 / 应用`
- 例如：「产品发布会 工程学 项目管理 视角」、「品味 经济学 信号理论 解读」

**Step 2 — 执行搜索**：
- 工具：联网搜索（如 `WebSearch`）
- 每个核心概念执行 2-3 次搜索（同领域 1-2 次 + 跨领域 1 次）
- 获取 5-8 个高质量来源

**Step 3 — 故事化筛选与串讲**：

不要按"支持/反对/延伸/迁移"分类。而是：

1. **从搜索结果中挑选最有故事性的案例**（有具体人物、时间、场景、转折）
2. **每个案例写一个完整的跨领域故事**，格式：

```markdown
### 一、{故事标题}

> **🌱 种子**
> 因为笔记中提到了"{核心概念}"，这让我想到{跨领域案例}——{一句话概括关联}。

（展开讲完整故事：背景 → 冲突 → 行动 → 结果 → 洞察）

**✨ Aha 瞬间**

> "{从案例中自己提炼的洞察，格式：真正的XX，不是YY，而是ZZ}"

> 📎 参考文档链接：
> - {来源名} — {URL}
```

3. **故事数量**：3-5 个，覆盖不同领域
4. **故事之间要有串联感**：后面的故事可以回应前面的洞察，形成递进

**Step 4 — 金句从故事中生长**：
- 不要引用别人的金句
- 每个故事讲完后，**自己提炼**一句 Aha 瞬间
- 格式：用引号包裹，像一句可以被带走的话

---

### 第三层：专家拷问（Pressure Test）

**目标**：像一位严厉的教练，直接指出笔记中的逻辑漏洞，帮你把想法想透。

**核心原则**：
- **不是结构化表格**，而是**对话式拆解**
- **直接指出逻辑谬误**，不要绕弯子
- **拆解对立关系**：如果笔记把两个不冲突的东西对立了，直接说
- **追问前提和代价**：每个判断都有隐藏前提和未计算的代价
- **最后加一个动机追问**：逼用户厘清自己的真实立场

**镜片选择**：拷问默认使用「段永平式（本质维度）+ 快刀青衣式（落地维度）」双镜片；可按笔记类型换用其他镜片，完整镜片定义与选择矩阵见 `references/expert-lens-registry.md`。

**拷问步骤**：

**Step 1 — 复述核心判断**：
先用 1-2 句话概括笔记的核心判断，像教练在确认"我理解的对吗？"

**Step 2 — 逐层拆解**：
按以下顺序展开质疑（不需要每个都用，但至少要覆盖 3-4 个维度）：

1. **逻辑谬误**："这个判断有个偷换概念的地方" / "你把两个不冲突的东西对立起来了"
2. **前提追问**："这个判断的前提是XX，但如果前提不成立呢？"
3. **代价计算**："你只算了A账，没算B账" / "观众的代价、市场的代价、你读者的代价"
4. **概念澄清**："你说的'XX'，到底是A还是B？这两个是不一样的"
5. **边界追问**："这个结论在什么情况下不成立？"
6. **动机追问**："你真正佩服的是A，还是B？这两个是不一样的"

**Step 3 — 语气**：
- 像真人在对话，不是机器在提问
- 可以用反问句、排比句增加力度
- 适当使用"说实话"、"其实"、"但问题是"等口语化衔接
- **不要表格，不要分点，要像一段连续的对话**

**Step 4 — 最后总结**：
用 1 句话总结最核心的追问："所以真正的问题是：..."

---

## 输出：发芽报告

### 报告逻辑（阅读顺序）

发芽报告按「**先快速回顾你在说什么，再看你的知识库里还藏着什么关联，然后用故事帮你看见更大的世界，最后逼你想深一层**」的逻辑排布：

1. **种子摘要**（2-3 句话）——快速确认地基
2. **本地笔记的洞察**（知识库关联网络）——这是本地知识库的独特优势，纯联网 AI 工具做不到。用叙述性洞察（不用表格）展示关联，每条洞察附上分析所依据的原文链接
3. **网络洞察**（跨领域故事串讲，3-5 个故事）——每个故事是「🌐 网络洞察」二级目录下的三级目录，有种子引子 + 完整展开 + Aha 瞬间，**Aha 之后附参考文档链接**
4. **专家拷问**（对话式拆解）——直接指出逻辑漏洞，追问前提和代价
5. **发芽建议**（2-3 个可执行动作）——基于上述发现的具体下一步

### 文件位置

发芽报告默认生成在**原笔记所在目录**（与笔记放一起，就近查看）：

```
{原笔记所在目录}/{YYYYMMDD}-{主题}-发芽报告.md
```

- `YYYYMMDD`：生成日期
- `{主题}`：原笔记标题去掉日期前缀（如有）后的主题名

> 想统一存放？告诉 AI 一个报告目录即可（如「发芽报告都放到 Notes/Sprout/ 下」）。

### 报告结构

```markdown
---
id: {原笔记id}
source_note: "[[YYYYMMDD-主题]]"
generated_at: YYYY-MM-DD HH:MM
sprout_version: v2.0
status: draft
---

# 发芽报告：{主题}

> 来源笔记：`[[YYYYMMDD-主题]]`
> 生成时间：YYYY-MM-DD HH:MM
> 本报告由 hehe-note-sprout 自动生成，仅供参考，不修改原笔记。

---

## 🌱 种子摘要

（用 2-3 句话概括原笔记的核心判断，像教练复述你的观点）

---

## 🔗 本地笔记的洞察

> 这是你的知识库里的关联发现——纯联网 AI 工具做不到这个。每条洞察都附上了分析时读到的原文链接，可点回去对照。

（先用 1-2 段总述：把这一批关联笔记放在一起看，浮出了一个什么整体 pattern？
例如："这批笔记都围绕同一主题展开，其中 XX 是唯一面向 YY 的视角"。
判断一律基于库内笔记对比得出，属于本地洞察——**不要单独用「关联网络洞察」之类的子标题**，总述直接挂在 H2 下。
总述段落后可用引用块附「📎 佐证来源」：如果判断引用了新闻稿/公开课/文章等外部公开记录，附上可追溯的 URL。）

### 一、{一句话点出这条洞察的结论}

（叙述性展开，不要用表格。每条洞察末尾用「——原文：[[笔记名]]」附上分析所依据的原文链接。）

#### 二、{...}

（同上）

#### 三、{...}

（同上）

---

## 🌐 网络洞察

> 这是从互联网检索到的跨领域案例与权威来源。每个故事作为「网络洞察」下的三级目录（### 级别），且每个「Aha 瞬间」之后必须附「📎 参考文档链接」引用块（可追溯的 URL），方便用户追溯。

### 一、{跨领域故事1}

> **🌱 种子**
> 因为笔记中提到了"{核心概念}"，这让我想到...

（展开讲完整故事：背景 → 冲突 → 行动 → 结果 → 洞察）

**✨ Aha 瞬间**

> "{从案例中自己提炼的洞察}"

> 📎 参考文档链接：
> - {来源名} — {URL}

---

### 二、{跨领域故事2}
...

### 三、{跨领域故事3}
...

（共 3-5 个故事，覆盖不同领域，故事之间有机串联；每个故事都要附参考文档链接）

---

## 🔍 拷问

（直接对话式质疑，像一位严厉的教练在跟你对话）

你这条笔记的核心判断是——"{复述核心判断}"。

这个判断有个问题：{直接指出逻辑谬误}...

还有，{追问前提/代价/概念澄清}...

最后问你一个：{动机追问}...

所以真正的问题是：{一句话总结最核心的追问}

---

## 💡 发芽建议

（基于本次发芽的具体发现，给出 2-3 个可执行的下一步动作）

- [ ] {具体动作1} —— 对应{某个故事的某个洞察}
- [ ] {具体动作2} —— 对应{拷问中的某个追问}
- [ ] {具体动作3} —— 对应{某个本地笔记的洞察}

> 本地笔记关联的快捷链接详见原文末尾「发芽」区块（若已执行发芽标记）。
```

> **注**：若执行了发芽标记，报告 frontmatter 的 `source_note` 与正文「来源笔记」中的链接会在全库 wikilink 同步时自动更新为带 🌱 的新文件名。

---

## 安全闸门

- **绝不修改原文主体内容**（正文观点、论述、案例保持原样；用户授权后，末尾追加发芽区块属于标准收尾动作，不污染正文）
- **发芽标记（改名 + 全库 wikilink 同步）首次执行前必须向用户确认**；用户明确授权后，后续可默认执行（除非用户当次明确说"只出报告不改卡片"）
- 外部搜索结果需标注来源和日期，不承诺权威性
- 专家拷问使用对话式语气，使用"某某式判断框架"，不声称名人在场
- 已发芽的笔记，在文件名末尾加 `🌱` 标识（wikilink 环境下同步更新全库链接）

---

## 参考文档

- `references/germination-protocol.md` — 三层循环详细规则
- `references/expert-lens-registry.md` — 专家镜片注册表（自包含，含各镜片框架、盲区与选择矩阵）
- `references/output-template.md` — 发芽报告模板
